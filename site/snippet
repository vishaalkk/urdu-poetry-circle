from email import message_from_file

fle = '/Users/vishalk/Desktop/07-15-2021.eml'
msg = message_from_file(open(fle))


import email
from email import policy
from email.parser import BytesParser
import glob
import os


fp = open(fle, 'rb')
msg = BytesParser(policy=policy.default).parse(fp)
text = msg.get_body(preferencelist=('plain')).get_content()
fp.close()

#class and following methods, and one of these methods would be binary search on xyz integers and would return that
#tables and write queries -- quality check 


# final round -- talk about high level architecture.